SELECT SupplierID, Country, HomePage FROM dbo.Suppliers WHERE HomePage IS NOT NULL AND Country = 'Albania' AND HomePage IS NOT NULL or Country = 'Andorra' AND HomePage IS NOT NULL or Country = 'Armenia' AND HomePage IS NOT NULL or Country = 'Austria' AND HomePage IS NOT NULL or Country = 'Belarus' AND HomePage IS NOT NULL or Country = 'Belgium' AND HomePage IS NOT NULL or Country = 'Bosnia and Herzegovina' AND HomePage IS NOT NULL or Country = 'Bulgaria' AND HomePage IS NOT NULL or Country = 'Croatia' AND HomePage IS NOT NULL or Country = 'Cyprus' AND HomePage IS NOT NULL or Country = 'Czechia' AND HomePage IS NOT NULL or Country = 'Denmark' AND HomePage IS NOT NULL or Country = 'Estonia' AND HomePage IS NOT NULL or Country = 'Finland' AND HomePage IS NOT NULL or Country = 'France' AND HomePage IS NOT NULL or Country = 'Georgia' AND HomePage IS NOT NULL or Country = 'Greece' AND HomePage IS NOT NULL or Country = 'Hungary' AND HomePage IS NOT NULL or Country = 'Iceland' AND HomePage IS NOT NULL or Country = 'Ireland' AND HomePage IS NOT NULL or Country = 'Italy' AND HomePage IS NOT NULL or Country = 'Kosovo' AND HomePage IS NOT NULL or Country = 'Latvia' AND HomePage IS NOT NULL or Country = 'Liechtenstein' AND HomePage IS NOT NULL or Country = 'Lithuania' AND HomePage IS NOT NULL or Country = 'Luxembourg' AND HomePage IS NOT NULL or Country = 'Malta' AND HomePage IS NOT NULL or Country = 'Moldova' AND HomePage IS NOT NULL or Country = 'Monaco' AND HomePage IS NOT NULL or Country = 'Montenegro' AND HomePage IS NOT NULL or Country = 'Netherlands' AND HomePage IS NOT NULL or Country = 'North Macedonia' AND HomePage IS NOT NULL or Country = 'Norway' AND HomePage IS NOT NULL or Country = 'Poland' AND HomePage IS NOT NULL or Country = 'Portugal' AND HomePage IS NOT NULL or Country = 'Romania' AND HomePage IS NOT NULL or Country = 'San Marino' AND HomePage IS NOT NULL or Country = 'Serbia' AND HomePage IS NOT NULL or Country = 'Slovakia' AND HomePage IS NOT NULL or Country = 'Slovenia' AND HomePage IS NOT NULL or Country = 'Spain' AND HomePage IS NOT NULL or Country = 'Sweden' AND HomePage IS NOT NULL or Country = 'Switzerland' AND HomePage IS NOT NULL or Country = 'Turkey' AND HomePage IS NOT NULL or Country = 'Ukraine' AND HomePage IS NOT NULL or Country = 'United Kingdom' AND HomePage IS NOT NULL or Country = 'UK' AND HomePage IS NOT NULL or Country = 'Vatican City' AND HomePage IS NOT NULL;